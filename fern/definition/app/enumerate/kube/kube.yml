imports: 
  requests: ../../../common/requests.yml
types:
  # AppEnumerateKube Struct
  AppEnumerateKubeAttemptInfo:
    properties:
      path: string
      request: requests.RequestInfo
  AppEnumerateKubeReport:
    properties:
      target: string
      attempts: optional<list<AppEnumerateKubeAttemptInfo>>
      errors: optional<list<string>>
