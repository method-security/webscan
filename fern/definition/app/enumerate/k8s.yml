imports: 
  requests: ../../common/requests.yml
types:
  # AppEnumerateK8s Struct
  AppEnumerateK8sAttemptInfo:
    properties:
      path: string
      request: requests.RequestInfo
  AppEnumerateK8sReport:
    properties:
      target: string
      attempts: optional<list<AppEnumerateK8sAttemptInfo>>
      errors: optional<list<string>>
