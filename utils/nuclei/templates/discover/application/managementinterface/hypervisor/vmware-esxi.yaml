id: vmware-esxi-detect
info:
  name: VMware ESXi Detection
  author: method-security
  severity: info
  description: Detects VMware ESXi web interfaces (including UI and MOB endpoints)
  metadata:
    method-application-type: MANAGEMENT_INTERFACE_HYPERVISOR
    method-module-name: VMWARE_ESXI
  tags: discovery,vmware,esxi,hypervisor
http:
  - method: GET
    path:
      - "{{BaseURL}}"
      - "{{BaseURL}}/ui/"
      - "{{BaseURL}}/mob/"
      - "{{BaseURL}}/folder"
      - "{{BaseURL}}/folder?dcPath=ha-datacenter"
    redirects: true
    max-redirects: 5
    stop-at-first-match: true
    matchers:
      - type: word
        part: body
        words:
          - "VMware ESXi"
          - "ESXi Embedded Host Client"
        case-insensitive: true
      - type: word
        part: header
        words:
          - "VMware ESXi"
          - "Server: VMware"
        case-insensitive: true