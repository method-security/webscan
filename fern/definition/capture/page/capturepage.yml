imports:
  requests: ../../common/requests.yml
  config: ../../common/config.yml
types:
  CapturePageConfig:
    properties:
      target: string
      takeScreenshot: boolean
      maxRedirects: integer
      insecure: boolean
      timeout: integer
      requestMethod: config.RequestMethod
      headlessConfig: optional<config.HeadlessConfig>
      browserbaseConfig: optional<config.BrowserbaseConfig>
  CapturePageReport:
    properties:
      request: optional<requests.RequestInfo>
      screenshot: optional<base64>
      config: CapturePageConfig
      errors: optional<list<string>>