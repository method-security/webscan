id: adminer-detect
info:
  name: Adminer Detection
  author: method-security
  severity: info
  description: Detects Adminer database administration UI using Adminer-specific login form fields and title markers.
  metadata:
    method-application-type: DATABASE_MANAGEMENT_APPLICATION
    method-module-name: ADMINER
    cpe: cpe:2.3:a:adminer:adminer:*:*:*:*:*:*:*:*
  tags: discovery,database,admin,adminer
http:
  - method: GET
    path:
      - "{{BaseURL}}"
      - "{{BaseURL}}/adminer.php"
      - "{{BaseURL}}/adminer/"
      - "{{BaseURL}}/mysql.php"
    redirects: true
    max-redirects: 5
    stop-at-first-match: true
    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200
      - type: dsl
        dsl:
          - 'contains(tolower(body), "<title>login - adminer") && contains(tolower(body), "name=\"auth[server]\"")'
          - 'contains(tolower(body), "name=\"auth[driver]\"") && contains(tolower(body), "name=\"auth[username]\"") && contains(tolower(body), "adminer")'
          - 'contains(tolower(body), "adminer.css") && contains(tolower(body), "name=\"auth[password]\"")'
