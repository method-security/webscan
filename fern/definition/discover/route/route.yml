imports:
  http: ../../common/http.yml
  method: ../../common/method.yml
types:
  # Function Config Struct
  RouteCaptureConfig:
    properties:
      target: string
      ignoreStaticAssets: boolean
      requireBaseUrlMatch: boolean
      spiderDepth: integer
      maxRedirects: integer
      insecure: boolean
      timeout: integer
      threads: integer
      requestMethod: method.RequestMethod
      headlessConfig: optional<method.HeadlessRequestConfig>
      browserbaseConfig: optional<method.BrowserbaseRequestConfig>
  # Report Struct
  RouteBodyParams:
    properties:
      name: string
      exampleValues: optional<list<string>>
  RouteQueryParams:
    properties:
      name: string
      exampleValues: optional<list<string>>
  RouteDetails:
    properties:
      baseURL: string 
      path: string
      method: optional<http.HttpMethod>
      queryParams: optional<list<RouteQueryParams>>
      bodyParams: optional<list<RouteBodyParams>>
  RouteCaptureReport:
    properties:
      target: string
      routes: optional<list<RouteDetails>>
      staticAssets: optional<list<string>>
      config: RouteCaptureConfig
      errors: optional<list<string>>