types:
  NucleiExpectedMatcher:
    properties:
      type: string
      value: list<string>
  NucleiProbe:
    properties:
      id: string # Nuclei Template ID
      payloads: optional<list<string>>
      expectedMatchers: optional<list<NucleiExpectedMatcher>>
